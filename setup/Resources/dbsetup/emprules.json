{
  "rules": {
    "db": {
      "employees": {
        ".read": true,
        ".write": true,
        ".indexOn": ["name", "email"],
        "$employee": {
          ".validate": "newData.hasChildren(['name','birthDate','city','department','email','gender','isActive','street','postal','state']) && newData.child('isActive').isBoolean() && newData.child('name').val() === data.child('name').val()"
        }
      }
    }
  }
}

